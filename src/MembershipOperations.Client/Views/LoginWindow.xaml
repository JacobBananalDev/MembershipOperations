<Window x:Class="MembershipOperations.Client.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sign In" Height="340" Width="420"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Membership Operations"
                   FontSize="20"
                   FontWeight="SemiBold"
                   Margin="0 0 0 16"/>

        <StackPanel Grid.Row="1" Margin="0 0 0 12">
            <TextBlock Text="Username" Margin="0 0 0 6"/>
            <TextBox Height="32"
                     Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0 0 0 12">
            <TextBlock Text="Password" Margin="0 0 0 6"/>
            <PasswordBox x:Name="PasswordBox" Height="32" PasswordChanged="PasswordBox_OnPasswordChanged"/>
        </StackPanel>

        <Button Grid.Row="3" Height="36"
                Content="Sign In"
                Command="{Binding LoginCommand}" />

        <TextBlock Grid.Row="4" Margin="0 12 0 0"
                   Text="{Binding StatusMessage}"
                   TextWrapping="Wrap"/>
    </Grid>
</Window>
