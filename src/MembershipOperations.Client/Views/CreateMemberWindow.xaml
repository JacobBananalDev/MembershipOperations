<Window x:Class="MembershipOperations.Client.Views.CreateMemberWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create Member" Height="360" Width="460"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="New Member"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Margin="0 0 0 14"/>

        <StackPanel Grid.Row="1" Margin="0 0 0 10">
            <TextBlock Text="First Name" Margin="0 0 0 6"/>
            <TextBox Height="32"
                     Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0 0 0 10">
            <TextBlock Text="Last Name" Margin="0 0 0 6"/>
            <TextBox Height="32"
                     Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0 0 0 10">
            <TextBlock Text="Email (optional)" Margin="0 0 0 6"/>
            <TextBox Height="32"
                     Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <TextBlock Grid.Row="4"
                   Margin="0 10 0 0"
                   Text="{Binding StatusMessage}"
                   TextWrapping="Wrap"
                   Opacity="0.75"/>

        <StackPanel Grid.Row="5"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0 16 0 0">
            <Button Content="Cancel"
                    Width="90"
                    Margin="0 0 10 0"
                    Command="{Binding CancelCommand}"/>
            <Button Content="Create"
                    Width="110"
                    Command="{Binding SaveCommand}"/>
        </StackPanel>
    </Grid>
</Window>
