<UserControl x:Class="MembershipOperations.Client.Views.MembersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0 0 0 10">
            <TextBox Width="260" Height="30"
                     Margin="0 0 10 0"
                     VerticalContentAlignment="Center"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />

            <CheckBox Content="Active only"
                      VerticalAlignment="Center"
                      IsChecked="{Binding ActiveOnly}" />

            <Button Content="Refresh"
                    Height="30"
                    Margin="12 0 0 0"
                    Command="{Binding RefreshCommand}" />

            <Button Content="Create"
                    Height="30"
                    Margin="12 0 0 0"
                    Command="{Binding CreateCommand}" />

            <Button Content="Edit"
                    Height="30"
                    Margin="8 0 0 0"
                    Command="{Binding EditCommand}" />

            <Button Content="Deactivate"
                    Height="30"
                    Margin="8 0 0 0"
                    Command="{Binding DeactivateCommand}" />
        </DockPanel>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Members}"
                  SelectedItem="{Binding SelectedMember, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsActive}" Value="False">
                            <Setter Property="Opacity" Value="0.55"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="70"/>
                <DataGridTextColumn Header="First Name" Binding="{Binding FirstName}" Width="*"/>
                <DataGridTextColumn Header="Last Name" Binding="{Binding LastName}" Width="*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="2*"/>
                <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="90"/>
                <DataGridTextColumn Header="Created" Binding="{Binding CreatedAtUtc}" Width="180"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="2"
                   Margin="0 10 0 0"
                   Text="{Binding StatusMessage}"
                   Opacity="0.75"/>
    </Grid>
</UserControl>
